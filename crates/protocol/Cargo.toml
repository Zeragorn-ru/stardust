[package]
name = "protocol"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Shared types for the launcher, auth-server and admin (manifest & API protocol)."

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
